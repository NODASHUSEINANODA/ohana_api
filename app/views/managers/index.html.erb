<div class="container my-5">
  <p class="h2 text-center">今月、誕生日を迎える方々</p>

  <table class="table table-striped table-hover">
    <thead class='bg-white'>
      <tr>
        <th>名前</th>
        <th>性別</th>
        <th>誕生日</th>
        <th>年齢</th>
        <th>住所</th>
        <th>勤続年数</th>
        <th>送り先</th>
        <th>メニュー</th>
        <th>退職・転職済み</th>
      </tr>
    </thead>
    <tbody>
      <% @birthday_in_next_month_members.each do |employee| %>
        <tr>
          <td class='align-middle small'><%= employee.name %></td>
          <td class='align-middle small'><%= employee.sex %></td>
          <td class='align-middle small'><%= employee.birthday_format_mm_dd %></td>
          <td class='align-middle small'><%= employee.age %>歳</td>
          <td class='align-middle small'><%= employee.address %></td>
          <td class='align-middle small'><%= employee.working_years %>年</td>
          <td class='align-middle small'>
            <%= select("dummy", "dummy_method", ["自宅", "会社"], {}, {class: "form-select w-100"}) %>
          </td>
          <td class='align-middle small'>
            <%# TODO: DBから値を取得する %>
            <%= select("dummy", "dummy_method", ["ライトプラン(3000円)", "スタンダードプラン(5000円)", "プレミアムプラン(8000円)"], {}, {class: "form-select"}) %>
          </td>
          <td class='align-middle small'>
            <%= check_box 'dummy', 'dummy_method', {class: "form-check w-75 text-center"}, true, false %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <%= button_to "更新", nil, class: "btn btn-primary w-100 my-3 py-2" %>
</div>